 // const { data:category, error,isPending } = useQuery({
  //   queryKey: ["topics"],
  //   queryFn: async () => {
  //     try {
  //       // const {data} = await axios.get(`${baseurl}/topics/?client_id=${api_key}&page=${page}&per_page=${per_page}&order_by=latest`);
  //       // const catag = data.map(topic => ({
  //       //   id: topic.id,
  //       //   title: topic.title
  //       // }));
  //       // console.log("data comming from hook"+ catag)
  //       // return catag;
  //       console.log(await axios.get(`${baseurl}/topics/?client_id=${api_key}&page=${page}&per_page=${per_page}&order_by=latest`))
  //       const resp = await axios.get(`${baseurl}/topics/?client_id=${api_key}&page=${page}&per_page=${per_page}&order_by=latest`);
  //       console.log(resp.data)
  //       const extractedCategories = resp?.data?.map((category) => ({
  //         id: category.id,
  //         title: category.title,
  //       }));
  //       console.log(extractedCategories)
  //       return(extractedCategories)
  //     } catch (error) {
  //       console.error("Error fetching topics:", error);
  //       return [];
  //     }
  //   },
  // });
  // const extractedCategories = category?.map((category) => ({
  //   id: category.id,
  //   title: category.title,
  // }));
  
  // console.log("Extracted categories:", extractedCategories);
  
  
// export const useGetTopics = () =>
//      useQuery({
//         queryKey: ['topics'],
//         queryFn: gettopics,
//       })
          {/* <View className=" w-full flex pt-5 pb-8">
            <Text className="text-lg font-normal text-gray-100 mb-3">
              Catagories
            </Text>
            <Text className="text-2xl  semibold text-white">Carousel</Text>
          </View> */}
          {/* <Catagories/> */}
          
{
  /* <Button
        title="Explore"
        onPress={() => router.push("/(tabs)/home")}
      /> */
}
//const router = useRouter()


          {/* <View className="flex flex-grow h-44">
            <FlatList
              horizontal
              // contentContainerStyle ={{paddingHorizontal:''}}
              showsHorizontalScrollIndicator={false}
              data={category}
              keyExtractor={(item) => item.id}
              estimatedItemSize={20}
              renderItem={({ item }) => (
                <Catagories title={item.title} id={item.id} photo={item.photo.thumb} />
              )}
            />
          </View> */}{/* <LottieView
        autoPlay={true}
        loop={true}
        renderMode={'SOFTWARE'}
        className ="w-full h-full absolute top-0 rounded-full "
        // style={{
        //   width: 500,
        //   height: 500,
        //   backgroundColor: '#eee',
        // }}
        source={require('../assets/work.json')}
      /> */}